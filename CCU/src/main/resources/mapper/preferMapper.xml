<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.team200.proj.dao.PreferDAO">
	<select id="getInfo" resultType="showVO">
		select id, name, mainposter, place_name, genre from showdb 
		order by id asc limit 0,200
	</select>
</mapper>